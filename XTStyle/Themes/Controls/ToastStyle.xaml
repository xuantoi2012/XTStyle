<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:XTStyle.Controls">

    <Style TargetType="{x:Type local:ToastNotification}">
        <Setter Property="MinWidth" Value="300"/>
        <Setter Property="MaxWidth" Value="400"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ToastNotification}">
                    <Border x:Name="border"
                            Background="{StaticResource CardBrush}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16"
                            RenderTransformOrigin="1,0.5">
                        <Border.RenderTransform>
                            <TranslateTransform x:Name="slideTransform" X="400"/>
                        </Border.RenderTransform>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="16" ShadowDepth="2" 
                                            Opacity="0.15" Color="#000000"/>
                        </Border.Effect>
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="slideTransform"
                                                       Storyboard.TargetProperty="X"
                                                       To="0"
                                                       Duration="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Status Bar -->
                            <Border x:Name="statusBar"
                                  Grid.Column="0"
                                  Width="4"
                                  Background="{StaticResource PrimaryBrush}"
                                  CornerRadius="2"
                                  Margin="0,0,12,0"/>

                            <!-- Icon -->
                            <TextBlock x:Name="icon"
                                     Grid.Column="1"
                                     Text="ℹ️"
                                     FontSize="20"
                                     VerticalAlignment="Center"
                                     Margin="0,0,12,0"/>

                            <!-- Message -->
                            <TextBlock Grid.Column="2"
                                     Text="{TemplateBinding Message}"
                                     FontSize="13"
                                     Foreground="{StaticResource TextPrimaryBrush}"
                                     TextWrapping="Wrap"
                                     VerticalAlignment="Center"/>

                            <!-- Close Button -->
                            <Button Grid.Column="3"
                                  Command="{TemplateBinding CloseCommand}"
                                  Width="20" Height="20"
                                  Margin="12,0,0,0"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  Cursor="Hand"
                                  VerticalAlignment="Top">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="closeBorder" 
                                              Background="{TemplateBinding Background}"
                                              CornerRadius="10">
                                            <TextBlock Text="✕" FontSize="10" 
                                                     HorizontalAlignment="Center" 
                                                     VerticalAlignment="Center"
                                                     Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="closeBorder" Property="Background" 
                                                      Value="{StaticResource BorderBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!-- Success Type -->
                        <Trigger Property="Type" Value="Success">
                            <Setter TargetName="statusBar" Property="Background" Value="{StaticResource SecondaryBrush}"/>
                            <Setter TargetName="icon" Property="Text" Value="✓"/>
                            <Setter TargetName="icon" Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
                        </Trigger>

                        <!-- Error Type -->
                        <Trigger Property="Type" Value="Error">
                            <Setter TargetName="statusBar" Property="Background" Value="{StaticResource DangerBrush}"/>
                            <Setter TargetName="icon" Property="Text" Value="✕"/>
                            <Setter TargetName="icon" Property="Foreground" Value="{StaticResource DangerBrush}"/>
                        </Trigger>

                        <!-- Warning Type -->
                        <Trigger Property="Type" Value="Warning">
                            <Setter TargetName="statusBar" Property="Background" Value="{StaticResource AccentBrush}"/>
                            <Setter TargetName="icon" Property="Text" Value="⚠"/>
                            <Setter TargetName="icon" Property="Foreground" Value="{StaticResource AccentBrush}"/>
                        </Trigger>

                        <!-- Info Type -->
                        <Trigger Property="Type" Value="Info">
                            <Setter TargetName="statusBar" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                            <Setter TargetName="icon" Property="Text" Value="ℹ"/>
                            <Setter TargetName="icon" Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
